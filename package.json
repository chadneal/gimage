{
  "name": "gimage-workspace",
  "version": "1.2.58",
  "private": true,
  "description": "Workspace for gimage project - actual npm package is in npm/ directory",
  "scripts": {
    "test": "cd npm && npm test",
    "publish-npm": "cd npm && npm publish"
  },
  "workspaces": [
    "npm"
  ]
}
